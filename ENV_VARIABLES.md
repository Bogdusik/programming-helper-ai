# 🔐 Переменные окружения для Vercel

## ✅ ОБЯЗАТЕЛЬНЫЕ переменные (должны быть установлены)

### 1. База данных
```
DATABASE_URL
```
**Описание:** URL подключения к PostgreSQL базе данных (Neon, Supabase, или другая)  
**Пример:** `postgresql://user:password@host:5432/database?sslmode=require`  
**Где взять:** Vercel Storage → Create Database → Neon (или ваша база данных)

---

### 2. OpenAI API
```
OPENAI_API_KEY
```
**Описание:** API ключ от OpenAI для работы AI чата  
**Где взять:** https://platform.openai.com/api-keys → Create new secret key

---

### 3. Clerk Authentication (обязательные)
```
CLERK_SECRET_KEY
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
```
**Описание:** Ключи для аутентификации через Clerk  
**Где взять:** https://dashboard.clerk.com → Your Application → API Keys

---

### 4. Clerk URLs (обязательные для правильной работы)
```
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/chat
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/chat
```
**Описание:** URL для редиректов после входа/регистрации  
**Значения:** Установите точно как указано выше

---

## ⚠️ КРИТИЧНО для админ-панели

### 5. Админ email
```
ADMIN_EMAILS
```
**Описание:** Email адреса, которые должны иметь админ-роль (через запятую)  
**Пример:** `bogdyn13@gmail.com`  
**Важно:** Без этой переменной админ-панель не будет работать!

---

## 📧 Опциональные (для отправки email)

### 6. Resend API (опционально)
```
RESEND_API_KEY
```
**Описание:** API ключ от Resend для отправки email  
**Где взять:** https://resend.com/api-keys  
**Примечание:** Нужен только если хотите отправлять email через форму контактов

### 7. Contact Email (опционально)
```
CONTACT_EMAIL
```
**Описание:** Email адрес для получения сообщений из формы контактов  
**Пример:** `your-email@example.com`  
**Примечание:** Нужен только если используете форму контактов

---

## 🎛️ Опциональные (настройки OpenAI)

Эти переменные имеют значения по умолчанию, но вы можете их переопределить:

```
OPENAI_MODEL=gpt-4
OPENAI_TEMPERATURE=0.7
OPENAI_MAX_TOKENS=2000
RATE_LIMIT_PER_MINUTE=60
```

---

## 📋 Полный список для копирования

Скопируйте эти переменные в Vercel:

```
DATABASE_URL=your-database-url-here
OPENAI_API_KEY=sk-your-openai-key-here
CLERK_SECRET_KEY=sk_live_your-clerk-secret-key
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_your-clerk-publishable-key
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/chat
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/chat
ADMIN_EMAILS=bogdyn13@gmail.com
RESEND_API_KEY=re_your-resend-key-here (опционально)
CONTACT_EMAIL=your-email@example.com (опционально)
```

---

## 🔍 Как проверить, что все установлено

После деплоя откройте:
```
https://your-project.vercel.app/api/diagnose
```

Этот endpoint покажет, какие переменные установлены, а какие нет.

---

## ⚠️ Важные замечания

1. **Все переменные с `NEXT_PUBLIC_`** должны быть установлены для **Production, Preview, и Development**
2. **Секретные ключи** (`CLERK_SECRET_KEY`, `OPENAI_API_KEY`, `DATABASE_URL`) должны быть установлены только для **Production**
3. **`ADMIN_EMAILS`** должен быть установлен для **Production** (иначе админ-панель не будет работать)
4. После добавления переменных **перезапустите deployment** в Vercel

---

## 🚀 Порядок установки

1. Установите все **ОБЯЗАТЕЛЬНЫЕ** переменные
2. Установите **ADMIN_EMAILS** (критично!)
3. Установите опциональные переменные (если нужны)
4. Сохраните и перезапустите deployment
5. После деплоя вызовите `/api/final-schema-sync`
6. Вызовите `/api/fix-admin-role` для создания пользователя

